<h1> VEM VACINA </h1>
<% if current_user %>
  Logged in as <%= current_user.cpf %>.
  <%= link_to "Log Out", logout_path %>
<% else %>
  <%= link_to "Cadastrar-se", new_endereco_path %> or 
  <%= link_to "Logar-se", login_path %>
<% end %>
<p id="notice"><%= notice %></p>

<% if current_user %>
  </br>
  <h5> USUARIO </h5>
  <%= link_to 'Show User', @current_user %>

<% else %>
  You need to login to gain access.
<% end %>